import { Link } from "react-router-dom";

function Navbar() {
    return (
        <div>
            <h2>
                <Link to={'/'}>Home</Link>
            </h2>
            
        </div>
    )
}

export default Navbar;